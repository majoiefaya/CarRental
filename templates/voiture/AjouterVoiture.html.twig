
{% extends 'base.html.twig' %}



{% block header %}
<div class="hero inner-page" style="background-image: url({{asset('images/hero_1_a.jpg')}})">
    <div class="container">
      <div class="row align-items-end ">
        <div class="col-lg-5">

          <div class="intro">
            <h1><strong>Ajout d'une Voiture</strong></h1>
            <div class="custom-breadcrumbs"><a href="{{path('Acceuil')}}">Acceuil</a> <span class="mx-2">/</span> <strong>Ajouter une Voiture</strong></div>
          </div>

        </div>
      </div>
    </div>
  </div>
{% endblock %}



{% block Description %}
    {% form_theme form 'bootstrap_4_layout.html.twig' %}
    {{ form_start(form) }}
    <div class="form-group">
    {{ form_widget(form) }}
    </div>
    <button class="btn btn-primary btn-sm">Sauvegarder</button>
    {{ form_end(form) }}

    <a href="{{ path('Voiture') }}" class="btn btn-outline-primary">Voitures</a>

{% endblock %}


    